import type { HasAverageColor, HasBackgroundColor, VirtualViewportPixelAccessor } from "../pixel-accessor";
import type { ReversePixelMapper } from "../pixel-mapper";
import type { ResampleFilterResolver } from "../resample-filter";
import type { ColorResampler } from "./ColorResampler";
import type { ColorResamplerFactoriesPool } from "./ColorResamplerFactoriesPool";
import type { ColorResamplerResolver, ColorResamplerResolverOptions } from "./ColorResamplerResolver";
/**
 * Base implementation of ColorResamplerResolver interface.
 */
export declare class BaseColorResamplerResolver implements ColorResamplerResolver {
    private readonly colorResamplerFactoriesPool;
    private readonly resampleFilterResolver;
    /**
     * BaseColorResamplerResolver constructor.
     *
     * @param colorResamplerFactoriesPool
     * @param resampleFilterResolver
     */
    constructor(colorResamplerFactoriesPool: ColorResamplerFactoriesPool, resampleFilterResolver: ResampleFilterResolver);
    /**
     * @inheritDoc
     */
    resolve(image: VirtualViewportPixelAccessor & HasAverageColor & HasBackgroundColor, reversePixelMapper: ReversePixelMapper, options?: ColorResamplerResolverOptions): ColorResampler;
    /**
     * Returns matching color resampler factory.
     *
     * @param preferredResampler
     * @param filter
     * @private
     */
    private getColorResamplerFactory;
}
