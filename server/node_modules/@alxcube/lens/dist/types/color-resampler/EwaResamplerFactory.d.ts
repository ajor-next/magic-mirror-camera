import { type ColorInterpolatorFactory } from "../color-interpolator";
import { type HasAverageColor, type HasBackgroundColor, type VirtualViewportPixelAccessor } from "../pixel-accessor";
import type { ReversePixelMapper } from "../pixel-mapper";
import type { ColorResamplerFactory, ColorResamplerFactoryOptions } from "./ColorResamplerFactory";
import { EwaResampler } from "./EwaResampler";
/**
 * EwaResampler factory class.
 */
export declare class EwaResamplerFactory implements ColorResamplerFactory {
    private readonly colorInterpolatorFactory;
    /**
     * EwaResamplerFactory constructor.
     *
     * @param colorInterpolatorFactory
     */
    constructor(colorInterpolatorFactory: ColorInterpolatorFactory);
    /**
     * @inheritDoc
     */
    readonly supportsEwa = true;
    /**
     * @inheritDoc
     */
    readonly requiresResampleFilter = true;
    /**
     * @inheritDoc
     */
    create(image: VirtualViewportPixelAccessor & HasAverageColor & HasBackgroundColor, reversePixelMapper: ReversePixelMapper, options?: ColorResamplerFactoryOptions): EwaResampler;
}
