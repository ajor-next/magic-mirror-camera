import { type ColorInterpolatorFactory } from "../color-interpolator";
import type { HasBackgroundColor, PixelAccessor } from "../pixel-accessor";
import type { ColorResamplerFactory, ColorResamplerFactoryOptions } from "./ColorResamplerFactory";
import { PointResampler } from "./PointResampler";
import type { ReversePixelMapper } from "../pixel-mapper";
/**
 * PointResampler factory class.
 */
export declare class PointResamplerFactory implements ColorResamplerFactory {
    private readonly colorInterpolatorFactory;
    /**
     * PointResamplerFactory constructor.
     * @param colorInterpolatorFactory
     */
    constructor(colorInterpolatorFactory: ColorInterpolatorFactory);
    /**
     * @inheritDoc
     */
    readonly supportsEwa = false;
    /**
     * @inheritDoc
     */
    readonly requiresResampleFilter = false;
    /**
     * @inheritDoc
     */
    create(image: PixelAccessor & HasBackgroundColor, reversePixelMapper: ReversePixelMapper, options?: ColorResamplerFactoryOptions): PointResampler;
}
