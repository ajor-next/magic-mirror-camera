import type { Color } from "../types";
/**
 * Color string parser interface.
 */
export interface ColorStringParser {
    /**
     * Parses color string and returns color channels array or undefined if string couldn't be parsed.
     *
     * @param colorString
     * @param quantumRange
     */
    parse(colorString: string, quantumRange: number): Color | undefined;
}
