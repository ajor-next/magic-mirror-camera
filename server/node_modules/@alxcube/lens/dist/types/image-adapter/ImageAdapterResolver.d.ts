import type { ImageAdapter } from "./ImageAdapter";
/**
 * ImageAdapter resolver interface.
 */
export interface ImageAdapterResolver {
    /**
     * Resolves image adapter for given resource.
     *
     * @param resource
     */
    resolve<ResourceType>(resource: ResourceType): ImageAdapter<ResourceType>;
}
