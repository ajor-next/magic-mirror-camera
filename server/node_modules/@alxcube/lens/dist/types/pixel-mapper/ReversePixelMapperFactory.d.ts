import { Viewport } from "../pixel-accessor";
import type { ReversePixelMapper } from "./ReversePixelMapper";
/**
 * ReversePixelMapper factory interface.
 */
export interface ReversePixelMapperFactory<T extends number[] = number[]> {
    /**
     * Creates mapper instance.
     *
     * @param args
     * @param viewport
     */
    create(args: T, viewport: Viewport): ReversePixelMapper;
}
