import type { ResampleFilterPresetsPoolKeyMap } from "./ResampleFilterPresetsPool";
import type { ResampleFilterPreset } from "./ResampleFilterPreset";
import type { ResampleFilter } from "./ResampleFilter";
/**
 * ResampleFilterResolver options.
 */
export interface ResampleFilterResolverOptions {
    /**
     * Resample filter blur. Only used for filter presets.
     * @default 1
     */
    filterBlur?: number;
    /**
     * Filter window support. Only used for filter presets.
     */
    filterWindowSupport?: number;
}
/**
 * ResampleFilter resolver interface.
 */
export interface ResampleFilterResolver {
    /**
     * Resolves resample filter.
     *
     * @param filter
     * @param options
     */
    resolve(filter: keyof ResampleFilterPresetsPoolKeyMap | ResampleFilterPreset | ResampleFilter, options?: ResampleFilterResolverOptions): ResampleFilter;
}
