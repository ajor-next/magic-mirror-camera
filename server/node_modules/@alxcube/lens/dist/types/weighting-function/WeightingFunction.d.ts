/**
 * Weighting function interface.
 */
export interface WeightingFunction {
    /**
     * Function for calculation color weights in ResampleFilter.
     *
     * @param x
     * @param support
     * @param windowSupport
     */
    (x: number, support: number, windowSupport: number): number;
}
